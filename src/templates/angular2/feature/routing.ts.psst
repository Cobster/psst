import { ModuleWithProviders } from '@angular/core';
import { Routes, RouterModule } from '@angular/router';

const $($Model.Name.Lowercase)Routes: Routes = [
    // { path: '', component: $($Model.Name)Component }
];

export const $($Model.Name)RoutingProviders: any[] = [

];

export const $($Model.Name)Routing: ModuleWithProviders = RouterModule.forChild($($Model.Name.Lowercase)Routes);